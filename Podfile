# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'iOS-Swift-MVC' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for iOS-Swift-MVC
  pod 'Alamofire'
  pod 'SwiftyJSON'
  
  target 'iOS-Swift-MVCTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'iOS-Swift-MVCUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
